1. First run it:

pip install -r requirements.txt

2. Run the following command:

python train.py

3. Finally run the following command:

python app.py

4. copy the link and open it on a browser and start chat with voice...